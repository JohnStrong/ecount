package spec

import org.specs2.mutable._

import play.api.test._
import play.api.test.Helpers._

import persistence.ecount.{PersistenceContext, AccountStore}
import PersistenceContext._
import persistence.ecount.AccountStore

object AuthenticationSpec extends Specification {

    /*
    val MOCK_USER_LOGIN_EMAIL = "example@example.com"
    val MOCK_USER_LOGIN_PASSWORD = "12345678A"

    "a user login should" should {
       "entering correct information" in {
         withConnection { implicit conn =>
            AccountStore.getAccountSaltAndHash(MOCK_USER_LOGIN_EMAIL) must not be none
         }
       }
    }
    */
}
